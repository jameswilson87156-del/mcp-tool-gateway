services:
  backend:
    build:
      context: ./backend
    ports:
      - "8080:8080"

  frontend:
    build:
      context: ./frontend
      args:
        VITE_API_BASE: http://localhost:8080/api
    depends_on:
      - backend
    ports:
      - "8088:80"
